# Vite 환경변수는 VITE_ 접두사가 반드시 필요합니다.
# dev 서버에서는 vite.config.ts 의 server.proxy 가 /api → localhost:9000 을 처리하므로
# 생성된 클라이언트의 URL 이 이미 /api/v1 로 시작하므로 dev 에서는 비워 둡니다.
# 프로덕션 빌드 시에는 실제 배포 URL 로 교체하세요.

VITE_API_BASE_URL=
